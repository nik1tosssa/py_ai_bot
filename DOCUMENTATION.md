# 📚 Полная техническая документация

## Содержание
- [Архитектура](#архитектура)
- [Модули проекта](#модули-проекта)
- [AI компоненты](#ai-компоненты)
- [FSM (Finite State Machine)](#fsm-finite-state-machine)
- [Обработка сообщений](#обработка-сообщений)
- [Датасеты](#датасеты)
- [Обучение и переобучение](#обучение-и-переобучение)
- [API и интеграции](#api-и-интеграции)
- [Примеры использования](#примеры-использования)

---

## 🏗️ Архитектура

```
┌─────────────────────────────────────────┐
│         Пользователи Telegram           │
└────────────────────┬────────────────────┘
                     │ Отправляют действие
                     ▼
        ┌────────────────────────┐
        │      main.py (Bot)     │
        │  aiogram Dispatcher    │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │    handlers.py         │
        │  - Приём сообщений     │
        │  - FSM состояния       │
        │  - Логирование         │
        └────────────┬───────────┘
                     │
                     ▼
        ┌────────────────────────┐
        │   predictor.py         │
        │   XPAnalyst класс      │
        │  - Анализ через NN     │
        │  - Расчет XP           │
        └────────────┬───────────┘
                     │
        ┌────────────▼───────────┐
        │   ai/models/           │
        │ complexity_model.keras │
        │   (обученная модель)   │
        └────────────────────────┘

┌──────────────────────────────────────────┐
│         Feedback Loop (Переобучение)     │
├──────────────────────────────────────────┤
│  ❌ bad_user_dataset.csv                 │
│  ✅ good_user_dataset.csv                │
│         (собираются в runtime)           │
│                    │                     │
│                    ▼                     │
│              teacher.py                  │
│        (переобучение модели)             │
│                    │                     │
│                    ▼                     │
│  Улучшенная complexity_model.keras       │
└──────────────────────────────────────────┘
```

---

## 📦 Модули проекта

### 1. **main.py** — Точка входа

```python
# Инициализация бота
bot = Bot(token=BOT_TOKEN, default=DefaultBotProperties(parse_mode=ParseMode.HTML))
dp = Dispatcher()  # Диспетчер aiogram
dp.include_router(router)  # Подключение обработчиков
await dp.start_polling(bot)  # Polling режим
```

**Основные объекты:**
- `Bot` — объект Telegram бота
- `Dispatcher` — маршрутизатор сообщений
- `Router` — набор обработчиков из handlers.py

**Параметры:**
- `ParseMode.HTML` — поддержка HTML разметки в сообщениях
- `start_polling()` — режим постоянного опроса сообщений

---

### 2. **config.py** — Конфигурация

```python
BOT_TOKEN = os.getenv("TELEGRAM_BOT_TOKEN")
AI_API_KEY = os.getenv("AI_API_KEY")
BASE_URL = os.getenv("BASE_URL")
```

**Переменные окружения (.env):**
| Переменная | Описание | Пример |
|---|---|---|
| `TELEGRAM_BOT_TOKEN` | Токен Telegram бота | `1234567890:ABCdef...` |
| `ADMINS_TELEGRAM_ID` | ID администратора для уведомлений | `123456789` |
| `AI_API_KEY` | Ключ для OpenAI-совместимого API | `lm-studio` |
| `BASE_URL` | URL сервера LM Studio | `http://10.14.0.2:1234/v1` |

---

### 3. **handlers.py** — Основная логика

**Структура файла:**
```
1. Импорты и инициализация
2. FSM (Finite State Machine) состояния
3. Вспомогательные функции
4. Обработчики (handlers)
```

#### **Вспомогательные функции**

```python
def clean_text(text: str) -> str
```
- **Назначение:** Очистка текста для нейросети
- **Преобразования:**
  - Нижний регистр
  - Удаление пунктуации
  - Нормализация пробелов
- **Пример:**
  ```python
  clean_text("Я подготовил доклад!") → "я подготовил доклад"
  ```

```python
def extract_action_text(message_text: str) -> str
```
- **Назначение:** Извлечение оригинального текста из сообщения бота
- **Использование:** Для логирования пользовательского действия

```python
def log_user_feedback(text: str, complexity: float, status: str) -> None
```
- **Назначение:** Логирование оценок в датасеты
- **Параметры:**
  - `text` — очищенный текст действия
  - `complexity` — сложность (0-10 или float)
  - `status` — "good" или "bad"
- **Результат:** Запись в CSV файл
  - `good` → `ai/dataset/good_user_dataset.csv`
  - `bad` → `ai/dataset/bad_user_dataset.csv`

#### **FSM (Finite State Machine)**

```python
class FeedbackStates(StatesGroup):
    waiting_for_complexity = State()
```

**Диаграмма состояний:**
```
Начало
  │
  ├─► /start → Ожидание текста действия
  │
  │   Пользователь отправляет действие
  │     │
  │     ▼
  │   Анализ нейросетью
  │     │
  │     ├─► Показать результат + кнопки
  │     │
  │     ├─ Пользователь нажимает ✅ → Логировать в good_dataset → Конец
  │     │
  │     └─ Пользователь нажимает ❌ → waiting_for_complexity
  │              │
  │              ▼
  │         Пользователь вводит новую сложность
  │              │
  │              ├─ /cancel → Отмена, Конец
  │              └─ Число 0-10 → Логировать в bad_dataset → Конец
```

#### **Основные обработчики**

```python
@router.message(CommandStart())
async def command_start_handler(message: Message)
```
- **Срабатывает:** При команде `/start`
- **Действие:** Приветственное сообщение

```python
@router.message(F.data.startswith("confirm_ok:"))
async def process_ok_rating(callback_query: CallbackQuery, state: FSMContext)
```
- **Срабатывает:** Когда пользователь нажимает ✅
- **Действие:**
  1. Логирует в `good_user_dataset.csv`
  2. Уведомляет админа об успехе
  3. Очищает состояние (state)

```python
@router.callback_query(F.data == "confirm_no")
async def process_bad_rating(callback_query: CallbackQuery, state: FSMContext)
```
- **Срабатывает:** Когда пользователь нажимает ❌
- **Действие:**
  1. Переводит в состояние `waiting_for_complexity`
  2. Просит пользователя ввести правильную сложность

```python
@router.message(FeedbackStates.waiting_for_complexity)
async def manual_complexity_input(message: Message, state: FSMContext)
```
- **Срабатывает:** В состоянии `waiting_for_complexity` при вводе числа
- **Действие:**
  1. Проверяет, что число в диапазоне 0-10
  2. Логирует в `bad_user_dataset.csv`
  3. Уведомляет админа о корректировке

---

### 4. **logger.py** — Логирование

```python
class Logger:
    def update_complexity(user_id: int, complexity: float) -> None
```

**Функциональность:**
- Отслеживает действия пользователей
- Обновляет статус в логах
- Может использоваться для рейтинга пользователей

---

## 🤖 AI Компоненты

### **ai/predictor.py** — Предсказание в реальном времени

```python
class XPAnalyst:
    def __init__(self, model_path, tokenizer_path)
    def analyze(self, text: str) -> float  # Возвращает сложность (0-10)
```

**Процесс анализа:**

```
1. Входной текст: "подготовил доклад по ИИ"
   │
   ▼
2. Предобработка (clean_text):
   "подготовил доклад по ии"
   │
   ▼
3. Токенизация (tokenizer.texts_to_sequences):
   [145, 322, 89, 12]  # ID слов
   │
   ▼
4. Padding (pad_sequences):
   [145, 322, 89, 12, 0, 0, ..., 0]  # MAX_LEN=30
   │
   ▼
5. Нейросеть (model.predict):
   prediction = [[7.2]]
   │
   ▼
6. Выход: complexity = 7.2
   XP = complexity * 100 = 720 XP
```

**Параметры модели:**
```python
MAX_LEN = 20           # Максимальная длина последовательности
MAX_WORDS = 10000      # Размер словаря
```

**Структура модели:**
```
Input (MAX_LEN=20)
  ▼
Embedding (10000 → 64)
  ▼
LSTM (64 units)
  ▼
Dense (32 units, relu)
  ▼
Dropout (0.3)
  ▼
Dense (1, linear)  ← Выход: сложность 0-∞
```

---

### **ai/teacher.py** — Обучение модели

```python
def load_data(path: str) -> pd.DataFrame
```
**Этапы загрузки:**
1. Проверка файла
2. Определение кодировки (UTF-16 или UTF-8)
3. Очистка имен колонок
4. Преобразование `complexity` в числа
5. Удаление пустых строк
6. Перемешивание (важно из-за мусора в начале файла!)

```python
def train_model(df: pd.DataFrame) -> None
```

**Этапы обучения:**

```
1. Подготовка данных
   - Разделение на train/test (80/20)
   - Токенизация текста
   - Padding последовательностей

2. Построение модели
   - Embedding слой
   - LSTM слой
   - Dense слои

3. Компиляция
   - Optimizer: adam
   - Loss: mse (Mean Squared Error)
   - Metrics: mae (Mean Absolute Error)

4. Обучение
   - Epochs: 50
   - Batch size: 32
   - Early Stopping: останавливает если не улучшается

5. Сохранение активов
   - Модель → ai/models/complexity_model.keras
   - Токенизатор → ai/tokenizers/tokenizer.pickle
```

---

### **ai/dataset_generator.py** — Генерация датасета

```python
def get_action(complexity: int) -> Optional[str]
```

**Сложность 0-10 шкала (OSE):**
```
0:   Моментальное действие без усилий
1:   Рутинное, не требует размышления
2:   Ежедневное базовое, 2 мин
3:   Ежедневное базовое, до 30 мин
4:   Ежедневное сложное или еженедельное базовое
5:   Еженедельное или сложное ежедневное
6:   Еженедельное сложное
7:   Ежемесячное сложное или еженедельное очень сложное
8:   Очень сложное, требует специальных навыков
9:   Очень очень сложное
10:  Максимально возможная сложность
```

**Категории действий:**
- Домашняя инженерия и электроника
- Агротехника и садоводство
- Кулинария и нутрициология
- Финансы и планирование
- Образование и сложные навыки
- Ремонт и реставрация
- Информационные технологии
- Здоровье и биохакинг
- Химия и физика в быту
- Организация пространства и логистика

**Процесс генерации:**
```
1. Выбрать случайную категорию
2. Запросить LM Studio через OpenAI API
3. Получить действие на русском языке
4. Очистить артефакты модели (regex)
5. Сохранить в новый датасет
6. Повторить N раз
```

---

## 📊 Датасеты

### **Структура CSV файлов**

```csv
text;complexity
очистил холодильник;2
написал статью о машинном обучении;7
приготовил завтрак;2
спроектировал архитектуру микросервисов;8
```

**Разделитель:** `;` (точка с запятой)  
**Кодировка:** UTF-16 (с BOM)

### **Файлы датасетов**

| Файл | Назначение | Обновляется |
|---|---|---|
| `dataset.csv` | Основной датасет для обучения | Вручную или скриптом |
| `good_user_dataset.csv` | Действия, подтвер. пользователями | Автоматически при нажатии ✅ |
| `bad_user_dataset.csv` | Неправильные оценки бота | Автоматически при нажатии ❌ и вводе числа |
| `new_dataset.csv` | Синтетические данные от LM Studio | При запуске dataset_generator.py |

---

## 🧠 Обучение и переобучение

### **Полный цикл**

```bash
# Шаг 1: Собрать действия от пользователей (происходит в runtime)
# Файлы автоматически создаются в ai/dataset/

# Шаг 2: Сгенерировать новые примеры
python ai/dataset_generator.py

# Шаг 3: Объединить датасеты (опционально)
# Можно вручную добавить строки из good/bad_dataset в dataset.csv

# Шаг 4: Переобучить модель
python ai/teacher.py

# Шаг 5: Перезагрузить бота
python main.py
```

### **Мониторинг обучения**

При запуске `teacher.py` выводится:
```
Epoch 1/50
32/32 [==============================] - 2s 50ms/step - loss: 2.3451 - mae: 1.2345 - val_loss: 2.1234 - val_mae: 1.1234
Epoch 2/50
...
```

**Важные метрики:**
- `loss` (train) — ошибка на тренировочном наборе (должна снижаться)
- `val_loss` (validation) — ошибка на тестовом наборе (должна снижаться)
- `mae` — средняя абсолютная ошибка (в сложности)

**Хорошие знаки:**
✅ val_loss < 1.5  
✅ mae < 1.0  
✅ Нет переобучения (val_loss не растет резко)

---

## 🔌 API и интеграции

### **Telegram Bot API (aiogram)**

```python
# Отправка сообщения
await message.answer("Привет!")

# Редактирование сообщения (для админа)
await bot.edit_message_text(
    chat_id=ADMIN_ID,
    message_id=message_id,
    text="Новый текст"
)

# Отправка клавиатуры
builder = InlineKeyboardBuilder()
builder.row(
    types.InlineKeyboardButton(text="✅", callback_data="confirm_ok"),
    types.InlineKeyboardButton(text="❌", callback_data="confirm_no")
)
await message.answer("...", reply_markup=builder.as_markup())
```

### **OpenAI-совместимое API (LM Studio)**

```python
from openai import OpenAI

client = OpenAI(base_url="http://10.14.0.2:1234/v1", api_key="lm-studio")

response = client.chat.completions.create(
    model="local-model",  # Модель в LM Studio
    messages=[{"role": "user", "content": prompt}],
    temperature=0.7,
    max_tokens=200
)
```

**Требования:**
- LM Studio должен быть запущен
- Модель должна быть загружена в LM Studio
- Endpoint: `http://10.14.0.2:1234/v1/chat/completions`

---

## 💡 Примеры использования

### **Пример 1: Простой анализ действия**

```python
from ai.predictor import XPAnalyst

analyst = XPAnalyst()
complexity = analyst.analyze("написал научную работу")
print(f"Сложность: {complexity:.1f}/10")
# Output: Сложность: 8.5/10
```

### **Пример 2: Логирование оценки**

```python
from handlers import log_user_feedback

log_user_feedback("написал научную работу", 8.5, "good")
# Запишет в ai/dataset/good_user_dataset.csv

log_user_feedback("написал научную работу", 7.0, "bad")
# Запишет в ai/dataset/bad_user_dataset.csv
```

### **Пример 3: Обучение новой модели**

```bash
# Terminal
python ai/teacher.py
```

```
Loading dataset...
Dataset size: 1,245 actions
Train set: 996 | Test set: 249
Building model...
Training...
Epoch 50/50: loss=0.8234, val_loss=0.9123
✅ Model saved to ai/models/complexity_model.keras
✅ Tokenizer saved to ai/tokenizers/tokenizer.pickle
```

### **Пример 4: Проверка датасета**

```bash
# Сколько строк в каждом датасете?
wc -l ai/dataset/*.csv

# Вывод:
#   1245 ai/dataset/dataset.csv
#    125 ai/dataset/good_user_dataset.csv
#     32 ai/dataset/bad_user_dataset.csv

# Посмотреть первые 5 строк
head -n 5 ai/dataset/dataset.csv
```

---

## ⚙️ Расширенная конфигурация

### **Параметры нейросети (teacher.py)**

```python
# Размеры сети
MAX_WORDS = 10000      # Размер словаря
MAX_LEN = 30           # Макс. длина последовательности
EMBEDDING_DIM = 64     # Размер эмбеддинга
LSTM_UNITS = 64        # Количество LSTM юнитов
DENSE_UNITS = 32       # Количество dense юнитов
DROPOUT = 0.3          # Dropout коэффициент

# Обучение
EPOCHS = 50            # Количество эпох
BATCH_SIZE = 32        # Размер батча
VALIDATION_SPLIT = 0.2 # 20% данных на валидацию
PATIENCE = 5           # Early stopping patience
```

### **Параметры генератора датасета**

```python
# dataset_generator.py
GENERATED_COUNT = 100  # Количество генерировать действий
TEMPERATURE = 0.7      # Креативность LM Studio
MAX_TOKENS = 200       # Макс. токенов в ответе
```

---

## 🔍 Исправление проблем

### **Проблема: Модель плохо предсказывает**

**Причины:**
1. Недостаточно данных (< 500)
2. Только синтетические данные
3. Дисбаланс в распределении сложности

**Решение:**
```bash
# 1. Собрать действия от реальных пользователей
# 2. Запустить генератор
python ai/dataset_generator.py

# 3. Объединить датасеты
cat ai/dataset/good_user_dataset.csv >> ai/dataset/dataset.csv

# 4. Переобучить
python ai/teacher.py

# 5. Проверить результаты
```

### **Проблема: Out of Memory при обучении**

**Решение:**
```python
# В teacher.py уменьшить batch size
BATCH_SIZE = 8  # Было 32

# Или уменьшить MAX_WORDS
MAX_WORDS = 5000  # Было 10000
```

---

## 📖 Дополнительные ресурсы

- **Aiogram документация:** https://docs.aiogram.dev/
- **TensorFlow/Keras:** https://www.tensorflow.org/guide/keras
- **OpenAI API:** https://platform.openai.com/docs
- **LM Studio:** https://lmstudio.ai/

---

**Версия документации:** 1.0.0  
**Обновлено:** February 2026

# 🤖 AI Complexity Analyzer Bot

> Telegram-бот с искусственным интеллектом для анализа сложности действий и начисления опыта (XP)

## 📋 Содержание
- [Описание](#описание)
- [Возможности](#возможности)
- [Требования](#требования)
- [Установка](#установка)
- [Конфигурация](#конфигурация)
- [Запуск](#запуск)
- [Использование](#использование)
- [Структура проекта](#структура-проекта)
- [Переобучение модели](#переобучение-модели)

## 📝 Описание

**AI Complexity Analyzer** — это умный Telegram-бот, который анализирует описания действий, задач и привычек пользователей, определяет их сложность по шкале 0-10 и начисляет опыт. Бот использует нейросеть для анализа и постоянно учится на основе обратной связи пользователей.

### Ключевая особенность
Это **feedback loop система** 🔄 — чем больше пользователи корректируют оценки, тем точнее становится модель.

## ✨ Возможности

✅ **Анализ сложности** — нейросеть определяет сложность действия (0-10)  
✅ **Расчет XP** — начисление опыта на основе сложности  
✅ **Интерактивное подтверждение** — пользователь может согласиться или исправить оценку  
✅ **Обучение на ошибках** — неправильные оценки логируются для переобучения  
✅ **Автогенерация датасета** — синтетическое расширение датасета через LM Studio  
✅ **Логирование** — отслеживание всех действий в отдельных датасетах  
✅ **Админ-уведомления** — отчеты для администратора о действиях пользователей  

## 🛠️ Требования

- Python **3.9+**
- pip (для установки зависимостей)
- Telegram Bot Token (получить у [@BotFather](https://t.me/botfather))
- OpenAI-совместимое API (например, LM Studio) для генерации датасета
- CUDA 12.x (опционально, для ускорения TensorFlow)

## 📦 Установка

### 1. Клонирование проекта
```bash
git clone <repo-url>
cd py_ai_bot
```

### 2. Создание виртуального окружения
```bash
python -m venv venv
```

**Windows:**
```bash
venv\Scripts\activate
```

**Linux/macOS:**
```bash
source venv/bin/activate
```

### 3. Установка зависимостей
```bash
pip install -r requirements.txt
```

## ⚙️ Конфигурация

Создайте файл `.env` в корне проекта:

```env
# Telegram Bot
TELEGRAM_BOT_TOKEN=YOUR_BOT_TOKEN_HERE
ADMINS_TELEGRAM_ID=123456789

# AI Analysis
AI_API_KEY=lm-studio
BASE_URL=http://10.14.0.2:1234/v1

# LM Studio (для генерации датасета)
# Убедитесь, что LM Studio запущен на http://10.14.0.2:1234
```

### Где взять токен?
1. Напишите [@BotFather](https://t.me/botfather) в Telegram
2. Отправьте `/newbot`
3. Следуйте инструкциям и скопируйте токен

### Где взять ADMIN ID?
1. Напишите боту `/start`
2. Ваш ID есть в коде: `message.from_user.id`

## 🚀 Запуск

### Обычный запуск
```bash
python main.py
```

Бот начнет слушать сообщения и будет готов к работе.

### Генерация синтетического датасета
```bash
python ai/dataset_generator.py
```

Убедитесь, что **LM Studio запущен** на адресе из `.env`.

### Обучение нейросети
```bash
python ai/teacher.py
```

Модель будет переобучена на основе текущего датасета и сохранена в `ai/models/`.

## 📱 Использование

### Для пользователя
1. Напишите боту `/start`
2. Отправьте описание действия: *"подготовил доклад по искусственному интеллекту"*
3. Бот анализирует и выдает сложность: **Сложность: 7/10**
4. Подтвердите ✅ или отклоните ❌
5. Если отклонили, введите свою оценку
6. Получите XP! 🎉

### Пример действий разных сложностей
- **0-1**: "погулял по дому"
- **2-3**: "приготовил завтрак", "помыл посуду"
- **4-5**: "изучил новый язык программирования", "написал статью"
- **6-7**: "подготовил доклад", "спроектировал архитектуру системы"
- **8-10**: "написал научную работу", "создал ML модель с нуля"

## 📁 Структура проекта

```
py_ai_bot/
├── main.py                          # Точка входа, запуск бота
├── config.py                        # Конфигурация (токены, переменные окружения)
├── handlers.py                      # Логика обработки сообщений
├── logger.py                        # Логирование пользовательских данных
├── requirements.txt                 # Зависимости Python
├── README.md                        # Этот файл
├── DOCUMENTATION.md                 # Полная техническая документация
│
└── ai/                              # AI компоненты
    ├── teacher.py                   # Обучение нейросети
    ├── predictor.py                 # Предсказание (класс XPAnalyst)
    ├── dataset_generator.py          # Генерация синтетического датасета
    │
    ├── models/
    │   └── complexity_model.keras    # Обученная модель (после teacher.py)
    │
    ├── tokenizers/
    │   └── tokenizer.pickle         # Токенизатор (после teacher.py)
    │
    └── dataset/
        ├── dataset.csv              # Основной датасет для обучения
        ├── good_user_dataset.csv    # Действия, подтвержденные пользователями
        └── bad_user_dataset.csv     # Неправильные оценки (для переобучения)
```

## 🧠 Переобучение модели

### Цикл улучшения

1. **Сбор данных**
   ```bash
   # Собираются автоматически при взаимодействии пользователей
   ai/dataset/good_user_dataset.csv  # ✅ Правильные оценки
   ai/dataset/bad_user_dataset.csv   # ❌ Неправильные оценки
   ```

2. **Расширение датасета**
   ```bash
   python ai/dataset_generator.py  # Сгенерировать новые действия
   ```

3. **Переобучение**
   ```bash
   python ai/teacher.py            # Обучить на новых данных
   ```

4. **Проверка результатов**
   - Перезагрузите бота: `python main.py`
   - Тестируйте с новыми действиями

### Рекомендуемый цикл переобучения
- Каждые **100-200 действий** пользователей → обновить датасет
- Каждые **500+ действий** → дополнить синтетическими примерами
- Каждые **1000+ действий** → полное переобучение модели

## 📊 Примеры команд

```bash
# Полный цикл переобучения
python ai/dataset_generator.py  # Добавить синтетику
python ai/teacher.py             # Обучить модель
python main.py                    # Запустить бота

# Просмотр датасета
head -n 5 ai/dataset/dataset.csv      # Первые 5 строк
wc -l ai/dataset/*.csv                # Количество строк в каждом файле
```

## 🐛 Решение проблем

### Ошибка: "❌ Ошибка: TELEGRAM_BOT_TOKEN не установлен!"
**Решение:** Добавьте `TELEGRAM_BOT_TOKEN` в `.env` файл

### Ошибка: "❌ Ошибка загрузки активов: No such file or directory"
**Решение:** Обучите модель:
```bash
python ai/teacher.py
```

### Ошибка при подключении к LM Studio
**Решение:** Убедитесь, что:
- LM Studio запущен
- Адрес в `.env` верный: `http://10.14.0.2:1234`
- Можете пропустить генерацию датасета, используя существующий

## 📚 Дополнительно

- **[DOCUMENTATION.md](DOCUMENTATION.md)** — полная техническая документация
- **[AI компоненты](ai/)** — детальное описание нейросети
- **[Handlers](handlers.py)** — логика обработки команд

## 📝 Лицензия

Этот проект использует MIT License.

---

**Автор:** nik1tosssa  
**Дата создания:** February 2026  
**Версия:** 1.0.0
